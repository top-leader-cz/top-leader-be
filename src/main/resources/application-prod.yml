server:
  port: {PORT}

spring:
  datasource:
    url: jdbc:postgresql:///top_leader
    username: top-leader
    password: past1520$A@B
    hikari:
      data-source-properties:
        socketFactory: com.google.cloud.sql.postgres.SocketFactory
        cloudSqlInstance: topleader-394306:europe-west3:top-leader-db
        enableIamAuth: false
        sslmode: disable
  mail:
    host: smtp.gmail.com
    port: 587
    username: jakubkrhovjak@gmail.com
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          starttls:
            enable: true
          auth: true

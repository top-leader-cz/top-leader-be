<?xml version="1.0" encoding="UTF-8"?>
<!-- Log4j2 config - JSON formatting is handled by Spring Boot structured logging (logging.structured.format.console=gcp) -->
<Configuration status="WARN">
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{username}] %-5level %logger{36} - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="CONSOLE" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
    </Appenders>

    <Loggers>
        <Root level="INFO">
            <AppenderRef ref="CONSOLE"/>
        </Root>
        <Logger name="org.springframework.web" level="INFO"/>
    </Loggers>
</Configuration>

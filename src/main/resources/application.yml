server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/top_leader
    username: root
    password: postgres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    placeholder-replacement: false
    baseline-on-migrate: true
    validate-on-migrate: true
  mail:
    host: smtp.gmail.com
    port: 587
    password: dummy pass
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          starttls:
            enable: true
            required: true
          auth: true

top-leader:
  app-url: http://34.36.149.115
  supported-invitations: en, cs, fr
  default-locale: en